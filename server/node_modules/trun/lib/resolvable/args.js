"use strict";
var inherits = require('util').inherits;
var Arr = require('./core/arr');


var Args = function (start, end) {
	Arr.call(this, start, end);
};
inherits(Args, Arr);

Args.prototype.getSrc = function (runner, ctx) {
	return ctx.args;
};

Args.args = new Args();


module.exports = Args;
