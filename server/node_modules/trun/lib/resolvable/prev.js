"use strict";
var inherits = require('util').inherits;
var Arr = require('./core/arr');


var Prev = function (start, end) {
	Arr.call(this, start, end);
};
inherits(Prev, Arr);

Prev.prototype.getSrc = function (runner, ctx) {
	return ctx.prevResult;
};

Prev.prev = new Prev();


module.exports = Prev;
